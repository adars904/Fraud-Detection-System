load_and_merge_data() 
  → clean_data() 
    → train_test_split_data() 
      → drop_high_missing_cols() 
        → check_skew() 
          → construct_features() 
            → split_features() 
              → build_encoding_transformer() + build_feature_selector()
                → build_fraud_pipeline() + build_cv_strategy()
                  → build_grid_search() 
                    → train_val_split_data() 
                      → grid_search.fit() 
                        → get_best_model() 
                          → select_best_threshold() 
                            → evaluate_model()  [RandomForest done]
                              → build_xgb_pipeline() + build_xgb_grid_search()
                                → grid_search_xgb.fit()
                                  → get_best_model() (xgb)
                                    → select_best_threshold() (xgb)
                                      → evaluate_model() (xgb)
                                        → compare_models()  [Final comparison]